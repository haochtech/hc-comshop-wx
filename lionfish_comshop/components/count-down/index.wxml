<view class="countdown-class i-count-down">
	<slot></slot>
	<block wx:if="{{showDay&&time.day>0}}">		<em class="em">{{time.day}}天</em>
	</block>
	<text class="item-class">{{time.hour}}</text>:<text class="item-class">{{time.minute}}</text>:<text class="item-class">{{time.second}}</text>
</view>
