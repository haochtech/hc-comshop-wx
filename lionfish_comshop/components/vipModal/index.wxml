<i-modal scrollUp="false" visible="{{visible}}">
	<view class="modal">
		<image bindtap="goUrl" class="img" mode="widthFix" src="{{imgUrl}}"></image>
		<view class="close">
			<text bindtap="closeModal" class="iconfont icon-guanbi"></text>
		</view>
	</view>
</i-modal>
